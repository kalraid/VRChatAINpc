spring:
  application:
    name: client-service
  rabbitmq:
      host: http://192.168.255.128
      port: 15672
      username: guest   
      password: guest
  datasource:
      driverClassName : org.mariadb.jdbc.Driver
      url : jdbc:mariadb://192.168.255.128:33301      
      username : root
      password : g@KgUnHhcYUYu7.
server:  port: ${PORT:8901}
eureka:
  client:
    service-url:      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}  

management:
   context-path: /manage
   security:
      enabled: false
   shell:
     ssh:
         enabled: true
         port: 18080
   health:
      diskspace:
         enable: false
      db:
         enabled: false
   endpoints:
      web:
         exposure:
            include: "*"

      